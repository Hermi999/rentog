require 'spec_helper'

describe PoolToolController do
  # The Pool Tool Controller just shows the Pool Tool page.
  # There are cucumber tests for showing the Pool Tool. Therefore we do not
  # have tests here.
  #
  # All interaction with the Pool Tool and the listings itself is handled by
  # the Transactions controller. You will find the tests there.
end
