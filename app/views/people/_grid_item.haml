.people-fluid-thumbnail-grid-item
  %div
    -# Listing image
    = render :partial => "layouts/grid_item_listing_image", :locals => {:listing => listing, :modifier_class => "people-listings", modifier_price: "", :modifier_image_marker => Listing.find(listing.id).get_listing_marker}
